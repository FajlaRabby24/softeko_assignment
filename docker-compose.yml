services:
  postgres:
    image: postgres:17-alpine
    container_name: team-project-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: interview_team_project
    ports:
      # Exposed so the Next.js app and `npx prisma migrate dev` / `studio` can
      # reach the database from your host.
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d interview_team_project"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
